# Copy this file to .env and fill in values. Never commit .env.

# Public URL of the deployed app. Used for QR code generation.
APP_URL=https://

# Public-facing URL shared with users (e.g. a custom domain once configured).
APP_PUBLIC_URL=https://

# Fly Managed Postgres cluster ID.
# Find it with: fly mpg list
MPG_CLUSTER_ID=

# Credentials for the telive app user (used by add-host-user-prod).
# Find in Fly dashboard > Postgres cluster > Connection details,
# or reset via: fly mpg connect <cluster-id> then ALTER ROLE telive WITH PASSWORD '...';
MPG_USER=telive
MPG_PASS=

# Password for the liquibase-user migration account (used by db-migrate-prod).
# Find in Fly dashboard > Postgres cluster > Connection details.
MPG_MIGRATE_PASS=